task :build do
    if File.exist? "empathy-0.0.1.gem"
        puts "removing old gemfile"
        File.delete "empathy-0.0.1.gem"
    end
    system "gem build empathy.gemspec"
    system "gem install empathy-0.0.1.gem"
end